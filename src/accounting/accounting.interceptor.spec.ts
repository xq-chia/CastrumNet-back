import { AccountingInterceptor } from './accounting.interceptor';

describe('AccountingInterceptor', () => {
  it('should be defined', () => {
    expect(new AccountingInterceptor()).toBeDefined();
  });
});
